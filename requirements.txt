# Inference dependencies — everything app/ needs to serve /predict.
# Training additionally needs requirements-train.txt.
#
# scikit-learn must stay pinned to the version that fitted
# models/harvestguard_scaler.pkl, or unpickling emits an
# InconsistentVersionWarning and the transform is not guaranteed to match.

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4

numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
joblib==1.5.3

tensorflow==2.21.0
keras==3.15.1
h5py==3.14.0
